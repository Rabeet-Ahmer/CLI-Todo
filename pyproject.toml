[project]
name = "todo_cli"
version = "0.1.0"
description = "A CLI Todo Application"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
dependencies = [
    "click>=8.0.0",
    "inquirer>=3.0.0",
]

[project.scripts]
todo = "main:cli"